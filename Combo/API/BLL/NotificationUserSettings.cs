
// Generated by MyGeneration Version # (1.3.0.3)

using System;
using Combo.DAL;
namespace Combo.BLL
{
	public class NotificationUserSettings : _NotificationUserSettings
	{
		public NotificationUserSettings()
		{
		
		}

        public bool CanGetNotification(int userID, int senderID, int NotificationType)
        {
            bool canNotified = false;
            if (IsColumnNull(ColumnNames.Status))
                canNotified = true;
            else if (Status == 2)
                canNotified = true;
            else
            {
                ProfileFollower follower = new ProfileFollower();
                if (follower.LoadByPrimaryKey(senderID, userID) && Status == 1)
                    canNotified = true;
            }
            return canNotified;
        }

        public virtual bool GetNotificationSettingsbyUserId(int userID) 
        {
            this.Where.ComboUserID.Value = userID;
            this.Where.ComboUserID.Operator = MyGeneration.dOOdads.WhereParameter.Operand.Equal;
            return this.Query.Load();
        }
    }
}
